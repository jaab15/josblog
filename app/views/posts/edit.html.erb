h1>Edit post</h1>

<%=form_for @post do |p| %>
<%= @post.errors.full_messages.join(',') %>

<div>
  <%= p.label :title %>
  <%= p.text_field :title %>
</div>

<div>
  <%= p.label :body %>
  <%= p.text_area :body %>
</div>

<%=   p.submit%>
<% end %>
